<?xml version="1.0" encoding="utf-8" ?>
<views:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:Samples.View"
             xmlns:viewmodels="clr-namespace:Samples.ViewModel"
             x:Class="Samples.View.CalendarPage"
             Title="Calendar">
    <views:BasePage.BindingContext>
        <viewmodels:CalendarViewModel />
    </views:BasePage.BindingContext>
    <StackLayout>
        <Label Text="Calendar Access Example"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
        <Button Text="Request Calendar Read Access"
                Command="{Binding RequestCalendarReadAccess}"/>
        <Button Text="Request Calendar Write Access"
                Command="{Binding RequestCalendarWriteAccess}"/>
        <Button Text="Load Calendars" 
                Command="{Binding GetCalendars}"/>
        <Label Text="{Binding Results}"/>
    </StackLayout>
</views:BasePage>